public enum Encounter {
    Uninitialized, Well, Tree, Chest,
}

public enum LocAttributes {
    animals,
    plants,
    magic,
    beauty
}

public enum Resource {
    Coins,
    Dust,
    Fairies,
    Fruit,
    Trinkets
}